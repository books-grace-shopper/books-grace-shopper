diff a/script/seedStory.js b/script/seedStory.js	(rejected hunks)
@@ -1 +1 @@
-const {User, Book, Order, Review, OrderBook} = require('../server/db/models');
+const {User, Book, Order, Review, OrderBook} = require('../server/db/models')
@@ -3,6 +3,6 @@ const {User, Book, Order, Review, OrderBook} = require('../server/db/models');
-async function guestAddsToCart(){
-  const newOrder = await Order.create();
-  const books = [];
-  for (let i = 1; i <= 10; i++){
-    const book = await Book.findByPk(i);
-    books.push(book);
+async function guestAddsToCart() {
+  const newOrder = await Order.create()
+  const books = []
+  for (let i = 1; i <= 10; i++) {
+    const book = await Book.findByPk(i)
+    books.push(book)
@@ -10,4 +10,4 @@ async function guestAddsToCart(){
-  const duplicateBook = await Book.findByPk(5);
-  await newOrder.requestBook(duplicateBook);
-  await newOrder.requestBook(duplicateBook);
-  return newOrder;
+  const duplicateBook = await Book.findByPk(5)
+  await newOrder.requestBook(duplicateBook)
+  await newOrder.requestBook(duplicateBook)
+  return newOrder
@@ -16,2 +16,2 @@ async function guestAddsToCart(){
-async function guestSignsUpWithCart(){
-  const order = await guestAddsToCart();
+async function guestSignsUpWithCart() {
+  const order = await guestAddsToCart()
@@ -20,3 +20,3 @@ async function guestSignsUpWithCart(){
-async function userMakesOrder(){
-  const user = await User.findByPk(5);
-  const order = await Order.findByPk(3);
+async function userMakesOrder() {
+  const user = await User.findByPk(5)
+  const order = await Order.findByPk(3)
@@ -25,2 +25,2 @@ async function userMakesOrder(){
-async function userBuysOrder(){
-  const user = await User.findByPk(7);
+async function userBuysOrder() {
+  const user = await User.findByPk(7)
@@ -29,2 +29,2 @@ async function userBuysOrder(){
-async function userCancelsOrder(){
-  const user = await User.findByPk(3);
+async function userCancelsOrder() {
+  const user = await User.findByPk(3)
@@ -38 +38 @@ module.exports = {
-  userCancelsOrder,
+  userCancelsOrder
